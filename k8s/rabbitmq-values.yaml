replicaCount: 1

auth:
  username: admin
  existingPasswordSecret: rabbitmq-auth
  existingErlangSecret: rabbitmq-auth

persistence:
  enabled: true
  size: 2Gi

service:
  type: ClusterIP 

plugins: "rabbitmq_management rabbitmq_pzrometheus"

extraConfiguration: |-
  management.path_prefix = /rabbitmq

ingress:
  enabled: true
  ingressClassName: nginx
  hostname: ""             
  path: /rabbitmq
  pathType: Prefix
  tls: false
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"

metrics:
  enabled: false  
